<main class="l-testimonies a-background">
    {{>@header}}

    <div class="l-testimonies__main ">
        {{>@main-title}}
        {{>@intro}}
        <span>
            {{>@input}}
        </span>
        <span>
            {{>@select}}
        </span>
        {{>@articles-part}}
        {{>@cta}}
    </div>

    {{>@footer}}
</main>